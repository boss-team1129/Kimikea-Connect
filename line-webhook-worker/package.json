{
  "name": "kimikea-line-webhook-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.25.0"
  }
}
